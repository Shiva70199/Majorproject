# Lightweight requirements for HuggingFace Inference API version
# No heavy ML dependencies needed - model runs on HuggingFace servers

# Web framework
Flask>=3.0.0
flask-cors>=4.0.0

# HuggingFace Hub library (handles Inference API automatically)
huggingface_hub>=0.20.0

# HTTP client (for fallback)
requests>=2.31.0

# Server
gunicorn>=21.2.0

